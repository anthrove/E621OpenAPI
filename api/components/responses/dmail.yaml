description: Success
content:
  application/json:
    schema:
    anyOf:
      - type: array
        items:
          $ref: "../schemas/dMail.yaml"
      - type: object
        description: No Results
        required:
          - dmails
        properties:
          dmails:
            type: array
            maxItems: 0
