description: Not Found
content:
  application/json:
    schema:
      type: object
      required:
        - success
        - reason
      properties:
        success:
          type: boolean
          enum:
            - false
        reason:
          type: string
          enum:
            - "not found"